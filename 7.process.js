// We can write this in the terminal: node 7.process.js --user "John" --greeting "Hello" hola emmanuel facebook deso
// console.log(process.argv)

// Controlar el proceso y su salida
// process.exit(1) // 0 is the default value, 1 is an error

// Control events
// process.on('exit', () => {
//   // For example clean the console
// })

console.log(process.cwd())
