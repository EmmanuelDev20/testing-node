// .js -> por defecto utiliza CommonJS
// .mjs -> por defecto utiliza ES Modules
// .cjs -> por defecto utiliza CommonJS
// .ts -> por defecto utiliza TypeScript

import { sum } from './sum.mjs'

console.log(sum(1, 2))